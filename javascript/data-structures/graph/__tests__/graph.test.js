'use strict';

const Graph = require('../lib/graph.js');

describe('====== Graph ======', () => {

  it('can successfully add a Node to the graph', () => {

  });

  it('can successfully add an edge to the graph', () => {

  });

  it('can properly retrieve a collection of all nodes from the graph', () => {

  });

  it('can return all the neighbors of a given node with the weight between nodes included', () => {

  });

  it('can properly return the size of the graph', () => {

  });

  it('a graph with only one node and edge can be properly returned', () => {

  });

  it('an empty graph properly returns null', () => {

  });

});
